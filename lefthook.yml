pre-commit:
  jobs:
    - name: frontend format and lint
      run: npx @biomejs/biome check ./frontend --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
      stage_fixed: true